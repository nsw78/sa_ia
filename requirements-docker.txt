# =============================================================================
# Requirements otimizado para Docker
# A plataforma educacional exibe exemplos de código em st.code(),
# não executa bibliotecas pesadas (torch, transformers) em runtime.
# Para desenvolvimento local com todas as libs, use requirements.txt
# =============================================================================

# Core - Streamlit app
streamlit>=1.30.0
python-dotenv>=1.0.0

# Data Science (usado em demos interativas)
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
scikit-learn>=1.3.0

# API & Web (referenciado nos exemplos)
fastapi>=0.104.0
uvicorn>=0.24.0
requests>=2.31.0
pydantic>=2.5.0

# Utilities
python-multipart>=0.0.6

# Monitoring / Healthcheck
prometheus-client>=0.19.0
